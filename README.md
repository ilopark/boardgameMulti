# 보드게임 (티츄 · 스컬킹)

친구들이랑 하려고 만든 실시간 웹 보드게임. 서버가 상태의 유일한 주인이고,
각 플레이어에게는 자기가 볼 수 있는 것만 마스킹해서 보낸다.

**운영 중:** https://boardplay.dpdns.org/ (Oracle Cloud VM · Docker · Socket.IO)

## 지금 상태

| 영역 | 상태 |
|---|---|
| 룰 스펙 (공식 룰북 검증) | ✅ [docs/RULES.md](docs/RULES.md) |
| 스컬킹 트릭 판정 · 점수 · 2인 유령룰 | ✅ |
| 티츄 조합 파서 · 봉황 와일드 · 되받아치기 | ✅ |
| 인원 | 티츄 4명 / 스컬킹 **2~6명** |
| 방 만들기 / 방코드 입장 / 자리 / 준비 · 봇 | ✅ 동작 확인 |
| 무작위 첫 딜러 · 딜러 회전 · 선턴 계산 | ✅ |
| 재접속 (새로고침해도 자리 유지) · 서버 재시작 복원 | ✅ 동작 확인 |
| 스컬킹 게임 진행 (입찰·플레이·점수) · 카드 UI | ✅ |
| 티츄 게임 진행 (그랜드·3장 교환·소원·폭탄·용/개·팀 점수) · 카드 UI | ✅ 서버 연결 완료 |
| 룰 엔진 · 서버 인프라 테스트 | ✅ **257개** (vitest) |
| 해적 고유 능력 (로지/바히즈/라스칼/후아니타/해리) | ❌ 옵션만 있음 (기본 off) |

## 실행

```bash
npm install
npm run dev
```

- 웹: http://localhost:5173
- 서버: http://localhost:3001 (헬스체크 `/health`)
- 같은 와이파이의 친구는 터미널에 찍히는 `Network:` 주소로 접속

```bash
npm test        # 룰 엔진 테스트
npm run typecheck
```

## 구조

```
packages/core/   순수 룰 엔진 — 서버·클라이언트 공용, 네트워크 의존 없음
  common/rng.ts     시드 기반 셔플 (테스트 재현 + 리플레이용)
  protocol.ts       소켓 이벤트 타입 (서버·웹 공용)
  skullking/        덱 · 트릭 판정 · 점수 · 판본별 옵션
  tichu/            덱 · 조합 파싱 · 되받아치기 판정
server/          Socket.IO — 방 관리, 상태의 유일한 주인
web/             Vite + React
docs/RULES.md    구현 계약서. 코드와 다르면 문서가 맞다.
```

## 설계 원칙

1. **룰은 순수 함수로.** `resolveTrick(plays, options)` 처럼 입력만 받아 결과를 낸다.
   네트워크·DB를 안 타서 테스트가 빠르고, 나중에 AI 봇도 같은 함수를 쓴다.
2. **서버가 상태의 주인.** 손패를 클라이언트에 전부 보내고 CSS로 가리는 짓은 안 한다.
   개발자도구 열면 다 보인다. 서버는 각자에게 자기가 볼 수 있는 것만 보낸다.
3. **판본·하우스룰은 옵션으로.** 스컬킹은 판본마다 보너스 점수가 다르다
   (인어→스컬킹: 2018판 50점 / 2021판 40점). 상수를 코드에 박지 않는다.
4. **룰 변경 순서는 문서 → 테스트 → 구현.** 반대로 하면 룰이 코드에 숨는다.

## 친구들과 하려면

```bash
brew install cloudflared   # 처음 한 번만
npm run build
npm run share              # 공개 링크가 뜬다
```

GitHub Pages로는 안 된다 (실시간 서버가 필요). 다른 배포 방법과 플랫폼 비교는
[docs/DEPLOY.md](docs/DEPLOY.md) 참고.

## 혼자 테스트하려면

방을 만들면 대기실에서 **방장이 "봇 추가" 버튼**으로 빈자리에 봇을 채울 수 있다.
봇은 항상 준비 상태이고, 게임이 시작되면 서버가 자동으로 대신 행동한다.
봇 옆의 × 버튼으로 언제든 내보낼 수 있다(대기실에서만).

```bash
npm run dev
# 방을 만든 뒤, 대기실에서 "봇 추가"로 빈자리를 채우면 된다
```

외부 프로세스로 봇을 붙이는 옛 방식(여러 서버에 붙이거나 스크립트로 자동화할 때):
```bash
npm run bots -- <방코드>
```

배포 형태 그대로 로컬에서 돌려보기:
```bash
docker build -t boardgame .
docker run --rm -p 3001:3001 -e NODE_ENV=production boardgame
```

## 다음 할 일

[docs/ROADMAP.md](docs/ROADMAP.md)
