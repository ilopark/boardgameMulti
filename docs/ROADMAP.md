# 로드맵

## 1단계 — 기반 ✅ (완료)

- [x] 공식 룰북 기준 룰 스펙 확정 ([docs/RULES.md](RULES.md))
- [x] 모노레포 (core / server / web), TypeScript strict
- [x] 스컬킹: 덱 · 팔로우 규칙 · 트릭 판정 · 크라켄/흰고래 · 보너스 · 점수
- [x] 티츄: 덱 · 조합 파싱 (봉황 와일드카드 포함) · 폭탄 · 되받아치기
- [x] 테스트 62개
- [x] Socket.IO 서버 + 방코드 입장 + 자리 + 준비 + 재접속
- [x] 턴 순서: 무작위 첫 딜러 · 딜러 회전 · 라운드별 선턴 · 자리 섞기

## 2단계 — 게임이 실제로 돌아가게 (스컬킹 완료)

핵심은 **게임 상태머신**. 트릭 판정 함수는 이미 있으니, "지금 누구 차례이고 무엇을 할 수 있는가"를 관리하는 층을 올린다.

### 스컬킹 (이쪽을 먼저 — 룰이 단순하고 인원 유연)

- [x] `packages/core/src/skullking/game.ts` — 라운드 상태머신
      `bidding → playing → trickEnd → roundEnd → 다음 라운드 → gameEnd`
- [x] 순수 리듀서 `reduce(state, action, rng)` — 액션은 `bid` / `play` / `advance`
- [x] 좌석별 마스킹 뷰 `viewFor(state, seat)` — 남의 손패는 장수만, 입찰은 전원 확정 후 공개
- [x] 서버 연결: `game:bid` / `game:play` / `game:ready` / `game:abort`, 각자에게 다른 뷰 전송
- [x] 웹: 손패 · 입찰 · 트릭 영역 · 점수판 · 라운드 결과 · 최종 순위
- [x] 티그리스 선언 모달
- [x] 2인 유령 손패 자동 플레이
- [ ] 해적 고유 능력 (로지/바히즈/라스칼/후아니타/해리) — 옵션은 있으나 미구현, 기본 off

### 티츄

- [ ] 8장 → 그랜드티츄 → 6장 → 카드 교환 → 티츄 → 플레이
- [ ] 마작 소원 강제 로직 (합법적으로 낼 수 있으면 반드시 내야 함)
- [ ] 개(파트너에게 리드 넘김), 용(트릭을 상대팀에게 넘김) 처리
- [ ] 폭탄 인터럽트 (자기 턴 아닐 때도 접수 — 트릭 확정 전까지)
- [ ] 원투 피니시 판정, 마지막 사람 손패/트릭 이관
- [ ] 팀 점수판 (1000점까지)

### 티츄 UI 다듬기 (완료)

- [x] 라이트/다크 모드 (localStorage 저장, 첫 방문은 OS 설정)
- [x] 교환 카드 선택 취소 (슬롯을 눌러도 취소)
- [x] 카드 크기 축소, 14장이 한 줄에
- [x] 차례 표시: ▶ 화살표 + "지금 차례" 라벨 + 테두리 강조
- [x] 패스 표시가 실제로 낼 때까지 유지
- [x] 실시간 점수 집계 (좌석별 · 팀별)
- [x] 라운드 전체 패스 (토글, 취소 가능. 리드/소원 때는 자동 해제)
- [x] 개를 낸 자리에 개 카드 표시
- [x] 티츄 선언 시 방 전체에 1.6초 알림
- [x] 교환에서 누가 뭘 줬는지 로그 (내 것만)

## 3단계 — 쓸만하게

- [ ] 인게임 채팅
- [ ] 판별 점수 히스토리 / 전적 (SQLite)
- [ ] 관전 모드
- [ ] 모바일 레이아웃 다듬기 (친구들 대부분 폰으로 할 것)
- [ ] 시드 기반 리플레이 (`createRng(hashSeed(방코드+라운드))` 이미 준비됨)

## 4단계 — 배포

- [x] 서버가 빌드된 프론트까지 서빙 (서비스 하나로 합침 → CORS 사라짐)
- [x] Dockerfile (멀티스테이지, non-root, 헬스체크)
- [x] GitHub Actions CI — 테스트·타입체크·이미지 빌드·컨테이너 기동 확인
- [x] 배포 가이드 [docs/DEPLOY.md](DEPLOY.md)
- [ ] 실제 배포 (Koyeb 무료 티어 권장)
- [ ] **방 상태를 메모리 → SQLite/Redis.** 지금은 재배포·슬립마다 진행 중인 게임이 날아간다

## 5단계 — AI 봇 (인원 부족할 때)

티츄는 정확히 4명이 필요해서 봇이 없으면 3명일 때 아예 못 논다.
룰 엔진이 순수 함수라 봇도 같은 함수를 그대로 쓸 수 있다.

- [ ] 스컬킹 봇 (쉬움 — 손패 세기 + 기대 트릭 수 계산)
- [ ] 티츄 봇 (어려움 — 조합 분해, 파트너 협조, 티츄 선언 판단)

---

## 알려진 이슈 / 결정 필요

- 방 상태가 인메모리 → 서버 재시작하면 진행 중인 게임이 사라진다 (2단계 끝나면 대응)
- 같은 브라우저의 두 탭은 localStorage를 공유해서 같은 플레이어로 취급된다.
  다른 기기/시크릿창을 쓰면 정상. 새 연결이 오면 이전 연결은 끊는다.
- 스컬킹 2인은 룰·옵션·좌석 순서까지 준비됐지만, 유령 손패를 실제로 굴리는 건 게임 상태머신(2단계) 작업이다
- [docs/RULES.md](RULES.md)의 "미확정 항목" 절 참고 — 흰고래 엣지케이스 등
